export default function Custom404({}) {
  // const [open, setOpen] = useState(false);
  // const node = useRef();
  // useOnClickOutside(node, () => setOpen(false));

  return (
    <div className="contact fourofour">
      <h2>Page Not Found</h2>
    </div>
  );
}
